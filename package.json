{
  "name": "invicti-works-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "predev": "node scripts/vendor-cms.mjs",
    "dev": "astro dev",
    "start": "npm run dev",
    "prebuild": "node scripts/vendor-cms.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "deploy": "npm run build && wrangler deploy",
    "cf:preview": "npm run build && wrangler dev",
    "test": "node worker/contact.test.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^7.2.4"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@sveltia/cms": "^0.196.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.125.0"
  }
}
